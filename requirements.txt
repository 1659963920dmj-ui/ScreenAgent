# ============================================================
# ScreenAgent 直接依赖清单
# Python 3.11.9 / GPU: RTX 4060 Laptop 8GB / CUDA 12.6
#
# 安装顺序（两步）：
#   1) 先装 CUDA 版 torch（PyPI 默认源无 +cu126 后缀，须用官方源）：
#      py -3.11 -m pip install torch==2.12.1 torchvision==0.27.1 \
#          --index-url https://download.pytorch.org/whl/cu126
#   2) 再装其余依赖：
#      py -3.11 -m pip install -r requirements.txt
# ============================================================

# ---- 深度学习框架（见顶部说明，不在本文件重复列 torch）----
# torch==2.12.1+cu126
# torchvision==0.27.1+cu126

# ---- 屏幕截图与控制 ----
pyautogui==0.9.54
mss==10.2.0
pynput==1.8.2
pyperclip==1.11.0

# ---- UI 元素识别（OCR）----
# PaddleOCR 3.x 的 paddlex 依赖检查要求 opencv-contrib-python 包名存在，
# 故用 contrib 版（opencv-python 的超集，同样提供 cv2）替代 opencv-python。
# opencv-python-headless 会由 easyocr 自动安装，同为 4.13 可共存。
opencv-contrib-python==4.13.0.92
paddlepaddle==3.3.1
paddleocr==3.7.0
easyocr==1.7.2

# ---- 大模型调用接口（src/llm）----
requests==2.34.2
PyYAML==6.0.2

# ---- Agent 框架 ----
langchain==1.4.0
langchain-community==0.4.2
llama-index==0.14.24

# ---- 模型与微调 ----
transformers==5.4.0
peft==0.20.0
accelerate==1.14.0
bitsandbytes==0.50.2
datasets==5.0.1
sentencepiece==0.2.2
safetensors==0.7.0

# ---- 数据与可视化 ----
numpy==2.1.3
pandas==2.3.0
matplotlib==3.10.3
seaborn==0.13.2
pillow==11.2.1
